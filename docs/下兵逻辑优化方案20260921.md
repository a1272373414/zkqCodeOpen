# 下兵逻辑优化方案（主世界 / 夜世界 / 都城）

> 生成日期：2026-09-21
> 来源项目：`D:\work\my\cocfz-apk-test`（脚本 `doc/decrypt/awcocx_main.lua`，竖屏 720×1280）
> 目标项目：`zkqCodeOpen`（横屏 1280×720，Kotlin）
> 关联文档：`docs/复用优化方案20260919.md`（11.3 的 90° 旋转坐标映射）

---

## 一、结论先行

| 模式 | 现状 | 主要问题 | 优化方向 |
|---|---|---|---|
| 主世界 | **✅ 已改造（2026-09-22）**：`universal/deploy/` 新模块 + `MainBaseDeployTroops.kt` 接入源体系 | 落点硬编码 `(600,50)→(85,430)`；无"四象限/波次/数量感知"；无真滑屏；`deployOthers` 只认"类别色"不认具体兵种 | 移植源"四象限 + 放兵方式 + 波次"体系，替换硬编码落点（**已完成，见 §九**） |
| 夜世界 | `BuilderBaseAttack.kt#normalBattle()` 固定候选点 + 随机游走 | 几乎不识别具体兵种；机器/英雄位置写死 `(125,610)`；无四象限 | 移植源"夜世界王识别 + 四象限"；用 `_bb_deploy.png` 派生部署栏兵种特征 |
| 都城 | **无下兵代码**（仅 `CapitalArmyTraining.kt` 配兵，且未被调用） | 攻打的"入口→搜索→下兵"整条链路缺失 | 移植源 `函数31a`（残血点驱动放兵）+ 已有 12 兵种/3 法术特征 |

**根因**：本项目只把源项目的"识别特征"迁移了过来，**没有迁移源的"下兵几何与编排"**（四象限落点、下兵方式、波次、放兵速度、援兵位置、双指真滑屏）。本方案补上这层。

---

## 二、源项目下兵架构（可复用资产）

来源文件 `awcocx_main.lua`，关键函数与行号：

### 2.1 战场被切成 4 象限
每个象限维护一组坐标：`开始X/Y`、`结束X/Y`、`中间X/Y`、`总长`，以及"走行/偏移"量。

竖屏基准点（L36500 附近）：
```
左上开始X/Y = 580, 507     右上开始X/Y = 580, 763
左下开始X/Y = 413, 237     右下开始X/Y = 413, 1029
```
- 之后按 `缩进直`、`右上走行`、`左上走行` 等做微调（L24822、L36520+）。
- `结束X = 开始X - 200 + 走行...`，`中间 = (开始+结束)/2`。
- **`XY差值 = 1.334134615384615`**（L2247）：竖屏下"横向 1 像素 ≈ 纵向 1.334 像素"的换算系数，源里所有 `xxx * _ENV["XY差值"]` 都靠它。
  → 迁到横屏 1280×720 时，这个系数与象限基准点都必须**重新标定**（不能照抄）。

### 2.2 下兵方式（`下兵方式`）四选一
来自配置（L73980 附近）：
- `四面`：上下两面各放一批（`放兵("上")` + `函数132a()` + `放兵("下")`，L36624）
- `单面仿滑屏`：单面、用 `函数327a` 模拟滑屏
- `单面真滑屏`：单面、用**双指 `touchDown/touchMoveEx` 真滑屏**（L36662+，数量 > 14 时启用）
- `单面中间单点下兵`：`单面中间单点下兵(位置, 兵x, 兵y, 数量)`（L36830），在象限中心单点连点

### 2.3 `援兵位置`（左上 / 右上 / 左下 / 右下）
决定援兵与主力的落点象限（L36632、36638）。

### 2.4 主放兵入口 `放兵(上下)`（L36872）
顺序：
1. **先下法术**：`下法术==true` 时，按 `findMultiColor` 逐个找法术图标（地震 `5A8ABD`、弹跳 `09D453`、狂暴…），找到即 `taps` 选法术 → `函数327a(上下, 数量, 类型)`。
2. **再下部队**：对每个兵种调 `函数142a(上下, 兵种, 数量, 色头, 色值)`。

### 2.5 `函数142a(上下, 兵种, 数量, 色头, 色值)`（L36650）
- `四面 / 单面仿滑屏` → 直接 `函数327a(上下, 数量, 兵种)`。
- `单面真滑屏`：
  - 数量 > 14 → **双指** `touchDown(1,..)/touchDown(2,..)` + 循环 `touchMoveEx(...)` 横扫（L36672+）。
  - 数量 ≤ 14 → 仍走 `函数327a`。

### 2.6 `函数327a(上下, 兵数, 放兵类型, 波)`（L37754）
- 按 **放兵类型** 给四象限设置 `偏移X/Y`（不同兵种/法术落点内缩量不同），例如：
  - 法术 `-30`、骷髅/蝙蝠 `-80`、弹跳 `-73`、地震 `-60`、冰冻 `-95`、隐形 `-50`、镜像 `-30`、蔓生 `-100`、毒药 `-130`、雷电/愤怒/图腾/炸弹 各有值（L37768–L37900）。
  - 偏移 = `-N`（X） 且 `+N * XY差值`（Y），把落点从象限"开始线"沿对角线内收。
- 支持 **波次 `波`=1..4+**（L37906）：每波额外 `a * XY差值` 的 Y 偏移，形成多波推进（L37924–37940）。

### 2.7 `放兵速度`
全局配置 `放兵速度`（默认 25，下限 15，L107143 / L109116），控制连点间隔；代码里大量 `sleep(_ENV["放兵速度"] + math.random(0,5))` 做随机化拟人。

### 2.8 夜世界：靠"夜世界王"定位（L31916）
```
夜世界王X, 夜世界王Y = findMultiColor(48, 15, 142, 500, "3067A2", "...", 0, 0.9)
if 夜世界王X > -1 then taps(夜世界王X, 夜世界王Y) end   -- 选中并下机器
```
即夜世界不是"扫整条部署栏"，而是**用战争机器的专属色特征在部署栏区域精确定位后点选**；技能释放另用 `FFB2FF`/`FE3AC7` 特征（L33272）。

### 2.9 都城：`函数31a` 残血点驱动放兵（L8403）
- 先识别"下兵点/放弃按钮"，统计特征数量决定是否可以开始放兵（L8413、8461）。
- 对每个 `都城下兵_*` 兵种：`函数26a(特征)` 命中 → `taps` 选兵 → 循环：遍历 `特征["残血"]`（L7505，低血敌人标记）找到就 `taps(下兵点)`（L8837–8960）。
- 法术（疗伤/弹跳/骷髅）单独走"选法术 → 找残血点 → 落法术"（L8879+，L94989+）。
- `偷袭法术下兵点`（L5977）/`偷袭冰冻法术下兵点`（L6841）为法术溅射点特征。

### 2.10 都城下兵特征格式（可直接转换）
`特征` 表（L5737–5977），格式 = `{x1,y1,x2,y2, 主色, 偏移串, 方向, 相似度}`：
```lua
["都城下兵_超级矿工"] = { 4, 28, 135, 1220, "7C9FEB",
  "-7|0|174B79|-8|-7|325C88|...", 0, 0.9 }
```
共 12 条：超级矿工、野猪偷袭队、超级巨人、超级法师、隐秘弓箭手、超级野蛮人、野蛮人攻城槌、骷髅法术、疗伤法术、雷电法术、弹跳法术、冰冻法术。
→ 与 `ColorSchema.parse(x1,y1,x2,y2,"主色","dx|dy|色,..",dir,sim,name)` 一一对应（仅分隔符 + 90° 旋转差异，见 §五）。

---

## 三、现状差距明细（逐文件）

### 3.1 主世界
`app/src/main/java/com/coc/zkqcode/jar/code/mainbase/attack/MainBaseDeployTroops.kt`
- 兵种：`deployIfPresent(...)` 固定清单（Dragon/Giant/Barbarian/Archer + 已迁移的 Prince/DragonRider/Totem）。
- 英雄：`deployHeroes()` 逐个重新截图扫描 16 个 schema。
- 其它：`deployOthers()` 只认类别色 `TroopColorAtDeploymentBar / SuperTroop... / SpecialTroop... / SpellColor... / ReviveSpell...`。
- **问题**：落点写死 `DRAG_START(600,50) / DRAG_END(85,430)`、`(310,247)/(386,222)`、`(328,366)`；无上/下分面；无波次；无数量；无真滑屏。

### 3.2 夜世界
`app/src/main/java/com/coc/zkqcode/jar/code/builderbase/attack/BuilderBaseAttack.kt#normalBattle()`
- 落点：`deployPositions` / `alternativeDeployPositions` 两组固定点随机取一。
- 机器：写死 `tap(125,610)`；无"夜世界王"特征定位。
- 仅区分"暗夜女巫 vs 野蛮人流"，不识别其它兵种。

### 3.3 都城
- `app/src/main/java/com/coc/zkqcode/jar/code/clancapital/CapitalArmyTraining.kt`：只有**配兵**（`trainCapitalArmy()` 等），且全仓库无外部调用者。
- `app/src/main/java/com/coc/zkqcode/jar/code/universal/tutorial/ClanCapicalTutorial.kt#clanCapitalTutorial()`：**空实现**。
- **缺**"进入都城 → 选目标 → 搜索/开打 → 下兵 → 结算"整条链路。

---

## 四、优化方案

### 4.0 新增通用基础设施（三模式共用）

建议新增目录 `app/src/main/java/com/coc/zkqcode/jar/code/universal/deploy/`：

1. **`DeployGeometry.kt`** —— 四象限几何
   - 把源竖屏基准点经 90° 旋转映射到 1280×720，并**用真机截图校准**出本项目自己的 `左上/右上/左下/右下` 基准点与 `XY差值`。
   - 提供 `quadrantStart/End/Middle(位置)`、`applyOffset(位置, dx, dy)`、`sweepLine(位置)`。
2. **`DeployStrategy.kt`**（枚举 `下兵方式`：`四面 / 单面仿滑屏 / 单面真滑屏 / 单面中间单点`）+ **`援兵位置`** + **`放兵速度`**（读 `Schema` 配置，默认 25，下限 15）。
3. **`DeployUnit.kt`** —— 统一部署单元：`特征 schema + 兵种名 + 数量来源 + 放兵类型(法术/骷髅/蝙蝠/弹跳/地震/冰冻/隐形/镜像/蔓生/毒药/雷电/愤怒/图腾/炸弹) + 波次`。
4. **`DeployExecutor.kt`** —— 执行器：
   - `函数327a` 等价：按类型设偏移、按波次推进、沿象限线连点。
   - `函数142a` 等价：数量 > 14 走**双指真滑屏**（复用 `TouchActions.touchDown/touchMove`，id=1、2），否则走按类型定向放兵。
   - 所有间隔套 `放兵速度 ± random` 拟人化（已有 `delayWithMultiplier`）。
5. **部署栏识别增强**：在"类别色"之外补**具体兵种色特征**（主世界/夜世界各自一份），并补"数量 `xN`"读取（复用 `PixelFontOcr`，见 `CapitalArmyTraining`）。

### 4.1 主世界（改造 `MainBaseDeployTroops.kt`）

**目标**：把"固定拖拽"升级为源同款"四象限 + 波次 + 数量感知"。

1. 用 `DeployExecutor` 取代 `dragUntilDeployed` 里的硬编码 `(600,50)/(85,430)`：
   - `下兵方式` 默认 `四面`：`放兵("上")`（左上+右上象限）→ 清场 `函数132a` → `放兵("下")`（左下+右下）。
2. `deployOthers()` 保留类别色做**兜底**，但优先用具体兵种 schema；命中后按 `函数327a` 的"类型偏移"落到对应象限，而不是写死 `(310,247)`。
3. 法术独立走"先选法术图标（`findMultiColor` + 类型）→ 按类型偏移落点"，支持 `ReviveSpellAtDeploymentBar` 等。
4. 数量 > 14 的兵种（如野蛮人/弓箭手）启用**双指真滑屏**。
5. 落点坐标不写死：全部由 `DeployGeometry` 用本项目标定值计算（源基准点仅作起点参考）。

### 4.2 夜世界（改造 `normalBattle()`）

1. **新增 `夜世界王` 特征**（源 `3067A2`，区域 48,15,142,500 竖屏 → 90° 旋转 + 校准到横屏），用 `findMultiColors` 在部署栏**精确定位战争机器**，替换写死的 `tap(125,610)`。
2. 用 `tools/_bb_deploy.png` 派生**部署栏各兵种色特征**，写入 `BuilderBaseAttackColors.kt`，让 `normalBattle` 能按兵种放兵（而非只分"女巫/野蛮人"）。
   - 先修 `tools/_crop_bb.py` / `_an.py` 的槽位坐标（当前 y 取到草地，见 §六）。
3. 保留"女巫流技能连点"与"野蛮人流游走"两条现有分支，但落点改由 `DeployGeometry` 提供（4 象限）。
4. 机器/部队落点不再用 `deployPositions` 硬编码表，改为象限随机点。

### 4.3 都城（新建下兵链路）

1. **新建 `app/.../clancapital/attack/CapitalAttack.kt`**，移植源 `函数31a`：
   - 识别"下兵点/放弃按钮" → 统计特征数达标 → 开始放兵。
   - 遍历 `都城下兵_*`（12 兵种）→ 命中选兵 → 遍历 `残血` 特征找目标点 → `tap` 放兵。
   - 法术（疗伤/弹跳/骷髅/雷电/冰冻）走"选法术 → 残血/偷袭点溅射"。
2. **特征迁移**：把 `awcocx_main.lua` L5737–5977（12 兵种+法术）、L5977/L6841（偷袭法术点）、L7505（`残血`）按 90° 旋转 + 分隔符转换，生成 `colorpackage/clancapital/CapitalDeployColors.kt`（`ICapitalDeployColors`）。
   - 转换脚本可复用 `tools/migrate_lua_colors.py`（已内置 11.3 映射）。
3. **接入流程**：`SceneState.CLAN_CAPITAL` 识别到都城后，接到 `CapitalAttack`（入口→目标→开打→下兵→回营）。填补 `clanCapitalTutorial()` 空实现。
4. 都城配兵 `trainCapitalArmy()` 接入攻打链路（当前无调用者）。

---

## 五、坐标与特征迁移规范

沿用 `复用优化方案20260919.md` §11.3（已验证）：
```
区域 (x1,y1,x2,y2) → (y1, 719-x2, y2, 719-x1)    # 取 min/max 并 clamp 1280×720
偏移 (dx,dy)       → (dy, -dx)
```
- 分隔符：源"点间 `|`、点内 `|`" → 本项目"点间 `,`、点内 `|`"（内含 `-101010` 后缀可保留或剥离，`ColorSchema.parse` 会忽略 `-` 之后内容）。
- ~~**注意**：`XY差值 = 1.334` 与四象限基准点是**竖屏专属**，横屏必须重新标定，不能照抄。~~
  **⚠️ 该结论已作废（2026-09-22 实测纠正）**：源脚本自带分辨率守卫
  `请把分辨率设置竖屏：720*1280-dpi：320`，而本项目运行在同一块屏的横屏态 1280×720 —— 两者是
  **同一矩形转 90°，像素 1:1**，`taps` 就是 `touchDown(0,x±1,y±1)+touchUp(0)` 无缩放。
  因此象限基准点与 `XY差值 = 1.334134615384615` **照搬即可**（`XY差值` 是基地菱形边的斜率，
  与屏幕方向无关）。换算只有一条：`realX = srcY`、`realY = 720 - srcX`（见 §九）。

---

## 六、已知坑与验证

1. **部署栏大小**：源多处提示"游戏设置-部署栏大小用默认大小"（L27474、L95619）——非默认大小会破坏所有落点与特征，需在方案中显式检查/提示。
2. **`_bb` 分析工具的 bug**（2026-09-22 已清理临时脚本，结论保留）：`_crop_bb.py` / `_an.py` 取槽位 `y=514..582` 是错的，实测夜世界部署栏卡片在 **`y≈575..700`**，取 `y=514..582` 会采样到草地（`375350` 等绿色）。开发夜世界（M3）时按 `y≈575..700` 重新切槽位；夜世界部署栏截图已归档为 `tools/captures/builderbase/bb_deploy.png`。
3. **离线受阻项**：文档 11.11 记录部署栏截图曾受阻；现已具备 `tools/captures/attack/live_bar_b1.png`（主世界）与 `tools/captures/builderbase/bb_deploy.png`（夜世界）。
4. **行为改动需真机验证**：源坐标旋转后对现版本是否仍适用，必须用 `emulator-5556` 实跑校准（参照 §11.3 的做法：多张截图交叉命中）。
5. **法术数量**：主世界当前造兵计划是"闪电法术 × 8"（文档 11.14 ⑥），下兵侧需与造兵计划对齐。

---

## 七、落地里程碑（建议顺序）

| 里程碑 | 内容 | 产出 |
|---|---|---|
| M1 | 通用基础设施 `DeployGeometry/Strategy/Executor` + 真机标定 1280×720 四象限 | 新增 `universal/deploy/*.kt` |
| M2 | 主世界接入四象限/波次/数量/真滑屏，去掉硬编码落点 | 改造 `MainBaseDeployTroops.kt` |
| M3 | 夜世界：夜世界王定位 + 部署栏兵种特征（先修 `_bb` 工具） | 改造 `normalBattle()` + `BuilderBaseAttackColors.kt` |
| M4 | 都城：迁移下兵特征 + 新建 `CapitalAttack.kt` + 接入流程 | 新增 `CapitalDeployColors.kt` / `CapitalAttack.kt` |
| M5 | 主世界造兵方案（训练侧）：5 方案下拉 + 解锁回退 + 蛮弓胖容量分配 + 飞龙系固定兵 + 手动跳过 | 新增 `MainBaseArmyPlan.kt` + 改造 `TrainTroops.kt` / `MainBaseSettings.kt` / `MainBaseConfigs.kt` |

每步都用 `tools/` 的验证脚本（`validate_legacy_deploy_colors.py`、`check_bar_hits.py`、`bar_templates.py`）在真实截图上核对命中率后再固化。

---

## 八、待办清单（勾选）

- [x] M1 四象限基准点与 `XY差值`（**无需标定**：1:1 旋转照搬，见 §九.1）
- [x] M1 `DeployGeometry` / `DeployStrategy` / `DeployExecutor` / `DeployUnit` / `DeployDebug`
- [x] M1 `函数327a` / `函数142a`（含数量 > 14 双指真滑屏）/ `函数147a~151a` 铺线
- [x] M2 主世界 `下兵方式` + `援兵位置` 配置项（Schema `deploy_mode` / `deploy_side`）
- [x] M2 主世界接入：去掉硬编码落点，兵种家族 / 英雄 / 通用兜底 / 攻城器 / 法术全部走源几何
- [x] M2 攻城器 9 种 + 援兵 5 个色特征迁移（`函数156a`）
- [x] M2 部署栏翻页放兵（`函数135a` / `函数136a`）
- [x] M2 数量感知：**取消** —— 用户决定不读界面数量；下兵维持 `count=16` + 轮次循环 + "栏里没了"兜底（已满足"把栏位兵下完"）
- [ ] M2 走行边界扫描（`函数121a/122a/131a/137a`）—— 实跑发现线偏时优先补
- [x] M3 夜世界王（战争机器）特征迁移 → `BuilderBaseAttackColors.BuilderBaseMachine`（源 `3067A2`，90° 映射后区域 `(15,577,500,671)`；在 `tools/captures/builderbase/bb_deploy.png` 实测命中 (104,635)）
- [ ] M3 夜世界部署栏兵种特征（从 `bb_deploy.png` 派生，槽位按 `y≈575..700`）
- [x] M4 都城 12 兵种 + 3 法术 + 残血/偷袭点特征迁移 → 新增 `colorpackage/clancapital/CapitalDeployColors.kt`（`ICapitalDeployColors`：12 个 `CapitalDeploy*` 下兵点特征 + `CapitalLowHpTargets`(残血 10) / `CapitalSneakSpellPoints`(偷袭法术 43) / `CapitalSneakFreezePoints`(偷袭冰冻 33)），已接入 `MyColors`；生成器 `tools/gen_capital_deploy_colors.py`（复用 `migrate_lua_colors` 的 90° 映射）。**离线无都城战斗截图**，命中待真机验证。
- [ ] M4 新建 `CapitalAttack.kt` 并接入 `SceneState.CLAN_CAPITAL`
- [x] M5 主世界造兵方案（训练侧）：`ARMY_PLAN` 下拉（蛮弓胖保底/闪电飞龙/镜像飞龙/图腾飞龙/手动配兵）；解锁检测未解锁则回退方案1；方案1 按兵营容量 1/3 分配蛮/弓/胖 + 闪电法术×20 + 余量补弓箭手；方案2-4 固定飞龙×20/气球×4/法术；方案4 跳过训练；不含英雄与攻城器（新增 `MainBaseArmyPlan.kt`，改造 `TrainTroops.kt`）
- [x] M5 校准 `MainBaseArmyPlan.CAPACITY_REGION` = `intArrayOf(580,153,666,179)`（"我的军队"页 1280x720 实测 "160/340"；**与源脚本一致**：源 `函数247a` 竖屏 `(540,400)-(566,666)` 经 11.3 的 90° 映射为横屏 `(400,153)-(666,179)`，右边界 666 与 y 带 [153,179] 完全相同，左边界取源"锚定分支"的图标右侧 ≈580）；并修复两处配套问题：① `PixelFontOcr` 增加"尺寸失配时按比例缩放重试"，使 12px 的容量字体（字库采自 15px 资源数字）能识别（`160?340`）；② `readTroopHousingTotal` 不再按 `'/'` 切分（斜杠不在字库），改为按数字分组取末组（同都城 `readCapacity`）。校验脚本 `tools/calibrate_capacity_region.py`。
- [x] M5 追加（容量读取增强）：① `PixelFontOcr` 切分 8-连通的**粘连数字**（`width > height*4/3` 时按"最小墨迹列"递归切开），修好真机 `340/340` 被读成 `?0?340`；② 用 `tools/harvest_slash.py` 采集**当前字体**的 `/` 字形替换旧字体遗留项，`/` 现可正常识别（主世界 `160/340`、都城 7 张 `X/250` 全对）；③ `MainBaseArmyPlan.readTroopHousing()` 返回 **(已用,总)**，`TrainTroops` 按 `总-已用` 分配兵（已用==总 即判满，与源脚本 `可训练 = 总单位 - 已训练` 同义）。真机 emulator-5556 实测 `340/340 → (340,340) 判满`。
- [ ] 下兵侧特殊兵种/法术处理（源脚本）：① 闪电法术优先对防空火箭释放；② 单面下兵时用飞龙/雷电飞龙断边（源 `飞龙清边/雷龙清边`、`闪火箭` 逻辑）—— 待接入 `DeployExecutor`

---

## 九、实施进度（2026-09-22）

### 9.1 关键纠正：坐标是 1:1 旋转，无需重新标定

源脚本自带分辨率守卫 `请把分辨率设置竖屏：720*1280-dpi：320`，`taps` 就是
`touchDown(0,x±1,y±1)+touchUp(0)`（无缩放包装）。本项目运行在同一块屏的横屏态 1280×720，
所以两者是**同一矩形转 90°，像素 1:1**：

```
realX = srcY
realY = 720 - srcX
```

→ 本文档 §二.1 的象限基准点与 `XY差值 = 1.334134615384615` **照搬即可**
（`XY差值` 是基地菱形边的斜率，与屏幕方向无关）。§五里"必须重新标定"的结论作废。

### 9.2 新增模块 `app/src/main/java/com/coc/zkqcode/jar/code/universal/deploy/`

| 文件 | 内容 |
|---|---|
| `DeployGeometry.kt` | 四象限线（开始/结束/中间/总长）、类型内缩 `偏移X/Y`、`函数147a~150a` 铺线、图腾波次、部署栏翻页手势、`sourceToReal` |
| `DeployStrategy.kt` | `DeploySide`(含 `isLeft/isTop`)、`DeployType`（源 `函数327a` 的内缩量 + 默认兵数）、`DeployMode`（四面/单面仿滑屏/单面真滑屏/单面中间单点）、`DeploySettings`（放兵速度 25、兵数 16、援兵位置） |
| `DeployExecutor.kt` | `deployFunction327a`、`deployFunction142a`（真滑屏双指）、`单面中间单点下兵`、`deployUntilGone`、`releaseSpells`、`deploySiegeOrReinforcement`（`函数156a`）、`swipeDeploymentBar`（`函数135a/136a`）、`resetDeployState` |
| `DeployUnit.kt` | 部署单元（特征 schema + 类型 + 默认数量） |
| `DeployDebug.kt` | `deploy_debug` 开关日志（HomeScreen 图标 + `GlobalSettings.DEPLOY_DEBUG`），日志以 `【下兵】` 前缀打印每一步决策 |

### 9.3 四象限几何（源种子 + `函数98a(缩进直=-60)`，已换算为横屏实际像素）

源种子：左上 `580,507` / 右上 `580,763` / 左下 `413,237` / 右下 `413,1029`；
`结束X = 开始X - 200`、`结束Y = 开始Y ± 200 × XY差值`（`总长 = 200 - 2×缩进直 = 320`）。

| 象限 | 开始 | 结束 | 中间 |
|---|---|---|---|
| 左上 | (587, 80) | (160, 400) | (374, 240) |
| 右上 | (683, 80) | (1110, 400) | (896, 240) |
| 左下 | (157, 247) | (584, 567) | (370, 407) |
| 右下 | (1109, 247) | (682, 567) | (896, 407) |

### 9.4 已迁移的源逻辑

- **`函数327a`**：四面 → 每象限 `兵数/4`；单面 → 全量落 `援兵位置`。按类型内缩
  （法术30 / 骷髅80 / 蝙蝠80 / 弹跳73 / 地震60 / 冰冻95 / 隐形50 / 镜像30 / 蔓生100 /
  毒药130 / **雷电150** / 冰障50 / 愤怒30；图腾按波取 100(波1) → +50 → +100 → +120 → +140）。
  保留源的例外分支：炸弹"半数→停1秒→半数+2"、地震单面 `≥5` 时 80/160 两排扇出、单面末尾
  `rnd(2,3)` 补点、"下"面四面分支额外 `rnd(2,3)` 补点。
- **`函数151a` / `函数147a~150a`**：沿象限线从 开始→结束 走 `兵数` 步，每步落在子段中点
  （`开始 + 偏移 - (前进 + 前进/2)`）—— 这就是原先缺失的"铺线"。
- **`函数142a`**：四面 / 单面仿滑屏 → 327a；单面真滑屏且 `数量 > 14` → 双指真滑屏
  （`touchDown` 在 中间±5、`touchMoveEx` 往返 开始↔结束，每轮查部署栏）；`≤14` → 327a。
- **`函数156a` + `找机器`/`找援兵`**：攻城器 9 种（各 2 个特征 = 18）+ 部落城堡援兵 5 个特征，
  共 **23 个色特征**按旋转口径迁移进 `MainBaseAttackColors.kt`；流程 = 找到卡 → 点卡 →
  点 `援兵位置` 象限的 `中间`；每局只放一次（源 `已放援兵`）。
- **`函数135a` / `函数136a`**：部署栏翻页（沿卡片行 `y=650` 横滑 `1150→250` 翻下一页，反向翻回）；
  每轮放完当前页后翻页继续放，最多 3 页（源 `兵种显示` 左/中/右三态），翻到无可识别兵种即右滑回第一页。
- **`放兵速度`**：25（下限 15，`+random(0,5)` 拟人）。

### 9.5 验证记录

- 编译：`gradlew :app:compileDebugKotlin` 与 `buildJar` 均 `BUILD SUCCESSFUL`，无 lint 诊断。
- 色特征：23 个攻城器/援兵特征在 21 张真实部署栏截图上核对 —— `SiegeSkyChariot` 命中
  `(227,602)`，裁图确认就是攻城机器卡（红色旋翼 + 木桶机身）；其余截图**零误命中**
  （不会误点法术 / 图腾 / 英雄卡）。
- 工程化：`app/build.gradle.kts` 的 `deployAndReload` 改为自动解析 SDK 全路径 adb +
  `-s emulator-5556`（可用 `local.properties` 的 `adb.serial` 覆盖），一条命令完成打包+推送+重载。
- 热更新：`encrypted_20016265.jar`（525,661 B，2026-09-22 02:44）已推送并发送 `DEBUG_RELOAD`。
- **待真机确认**：铺线落点是否有效（尤其顶部 `y≈80~120` 与两侧 `x≈157/1110` 的点击）、
  翻页手势是否被游戏识别为"栏内滚动"、攻城器是否真的被放出。

### 9.6 未完成 / 下一步

1. **走行边界扫描**（`函数121a/122a/131a/137a`）未迁移：当前用源兜底的 `走行==0` 种子 +
   `函数98a(-60)`。若实跑发现线偏，优先补这个扫描（或先调 `DeployGeometry.indent` / 种子微调）。
2. **数量未知**：源从配兵配置读 `兵数`，本项当前按 `DeploySettings.count = 16`
   （四面 = 每象限 4 次）铺线，靠"部署栏里没了"结束 → 可接 OCR 读 `xN`。
3. **法术时机**：源 `放兵` 是"先法术后部队"；本项目保留"部队中穿插单个法术"（仅落点改为
   源的 `中间 + 法术偏移`），需要严格照搬时可改。
4. **`函数132a`**（下滑部署栏 / 清场）未迁移，两批之间只按放兵速度等待。
5. **M3 夜世界、M4 都城**未开始。
